[project]
name = "marimo-notebooks"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

dependencies = [
    "marimo",
    "polars",
    "pandas",
    "altair",
    "psycopg[binary]",
    "pytest",
]
